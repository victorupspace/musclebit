import { AccountCreatedScreen } from '@/features/auth';

export default function AccountCreatedRoute() {
  // Ligar `onContinue` à entrada no app autenticado quando o grupo (app) existir.
  return <AccountCreatedScreen />;
}
